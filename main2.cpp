#include <iostream>
#include "coordinates/Point.h"

int main2() {
    Point matrix_size, startPoint, endPoint;
    char garbage;
    cin >> matrix_size >> garbage >> startPoint >> garbage >> endPoint;
    //GridControl *gc = new GridControl(matrix_size, startPoint, endPoint);
    delete (gc);
    return 0;
}